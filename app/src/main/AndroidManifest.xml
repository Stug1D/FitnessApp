<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:label="Eraay7"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddExerciseActivity"/>
        <activity android:name=".activities.CreateWorkoutActivity"/>
        <activity android:name=".activities.ExerciseListActivity"/>
        <activity android:name=".activities.AddWorkoutActivity"/>
        <activity android:name=".AddWeightActivity" />
        <activity android:name=".activities.CaloriesActivity" />
        <activity android:name=".activities.Workoutactivity" />
        <activity android:name=".activities.TemplateSelectionActivity"/>
    </application>

</manifest>